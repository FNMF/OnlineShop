namespace API.Domain.Services.PaymentPart
{
    public class PaymentFactory
    {
        /*
         * 貌似没什么好写的
         * 对于支付实体的创建，更多的是依赖于外部支付网关（如微信支付、支付宝等）的接口返回数据
         * 同时合法性验证也主要依赖于这些外部服务的验证机制
         */
    }
}
